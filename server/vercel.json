{
    "version":2,
    "builds": [
    {
      "src": "./index.js",  // Adjust the path to your server project
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}